[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "env_loader"
version = "0.1"
dependencies = [
  "python-dotenv==1.0.1"
]

[tool.setuptools.packages.find]
where = ["src"]

